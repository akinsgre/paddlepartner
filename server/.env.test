# Test Environment Configuration
NODE_ENV=test
JWT_SECRET=test-jwt-secret-key
PORT=3002

# MongoDB will be provided by mongodb-memory-server
# These are just fallbacks that won't be used
MONGODB_URI=mongodb://localhost:27017/paddle-partner-test

# Strava credentials (test values)
STRAVA_CLIENT_ID=test-client-id
STRAVA_CLIENT_SECRET=test-client-secret

# Client URL for testing
CLIENT_URL=http://localhost:3000